import "../styles/globals.css";
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sui Coffee Machine App',
  description: 'Sui Coffee Machine App',
}

export default function RootLayout(): any {
  return (
    <html lang="en">
      <head>
      </head>
        <body>
            Hello world
      </body>
    </html>
  );
}
